# 元组tuple使用小括号()标识
# (元素1, 元素2, ...)
# 多数特性与列表相同，但是不能修改元组中的元素
tuple1 = (1, 2, 3)

# 定义单元素的元组，需要加一个逗号
tuple2 = (1, )

# 定义空元组
em_tuple1 = ()
em_tuple2 = tuple()

# 元组的常用方法有index(),count(),len()
